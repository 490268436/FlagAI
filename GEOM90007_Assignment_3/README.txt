The root folder should contain two subfolders: data and script. The data folder contains all the data used by our application. The script folder contains R script, Tableau worksheet and other script files like css file for styling. 

For running our application, you should open and execute the group_assignment.R script file, which works as the entry point of our application.